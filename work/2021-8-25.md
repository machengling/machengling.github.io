### 1.开源软件升级代码记得提交，mediaAccess也有一个，也一并帮忙提交下吧，可以确认下mediaAccess是否可以下线呢，或者有方法屏蔽？
```
1.mediaaccess和billing的风险开源已经修复并提交
2.metric上的研发环境（风险开源），统计的是流水线上最近一次主干分支构建的记录。需要合入主干分支，跑一下流水线才可以改变metric的统计
```
### 2.确认自己的生成的gamma环境流水线metric能识别
```
1.metric现已经识别到billing的gamma测试
2.高风险变更统计的gamma失败次数，与metric上的统计略有不同。其统计的是流水线上绑定的gamma测试套运行的情况。
而metric是从cloudtest度量中心获取数据
```
### 3.性能问题单需要跟踪下

### 4.阿尔法覆盖率
```
1.负责覆盖率上传服务器的负责人一直不搭理，
2.目前看已经没有其他的容易在跑dev-tqb的镜像了。
3.同时在alpha集群的环境变量上加上了COVERAGE_ENABLE的开关，来判断是否上传覆盖率。（保证其他人误用了dev-tqb的镜像，也不会影响覆盖率的上传）
```
